{
  "name": "parsichin",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that makes mixed Persian/English text readable on AI chat web pages",
  "scripts": {
    "check": "bash scripts/check.sh",
    "build": "bash scripts/build.sh",
    "test": "node tests/smoke.test.js && node tests/ui-sanity.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
