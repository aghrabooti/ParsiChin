# Builds
dist/

# Node / tests (lockfile is committed for reproducible dev installs)
node_modules/

# OS / editors
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local download bundles (project zip + change patch) built for review
ParsiChin-*.zip
ParsiChin-*.patch
